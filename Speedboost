-- DELTA KICK SCRIPT
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

pcall(function()
    LocalPlayer:Kick(
        "Kulit mo ah, block mo pa ako sa live para di ko makita na nagamit ka ibang acc lol."
    )
end)
